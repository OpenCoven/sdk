name: ci

on:
  push:
    branches:
      - '**'
  pull_request:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
      - uses: pnpm/action-setup@fc06bc1257f339d1d5d8b3a19a8cae5388b55320 # v4.4.0
        with:
          version: 10.34.0
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version-file: .node-version
          cache: pnpm
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      # Build first. The examples and packages import each other across the
      # workspace, and their declaration files only exist once built. Linting
      # or typechecking before that leaves every cross-package import an
      # unresolved "error type", which the type-aware rules then report as
      # dozens of no-unsafe-* violations in code that is perfectly fine.
      - name: Build
        run: pnpm --recursive build
      - name: Lint
        run: pnpm lint
      - name: Typecheck
        run: pnpm typecheck
      - name: Test
        run: pnpm test
      - name: Verify authority fixtures
        run: node scripts/verify-contracts.mjs
      - name: Verify packed packages
        run: node scripts/verify-package.mjs
